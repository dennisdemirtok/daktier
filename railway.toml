[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn edge_app:app --bind 0.0.0.0:$PORT --workers 3 --threads 4 --worker-class gthread --timeout 120"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
